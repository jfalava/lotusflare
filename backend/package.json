{
  "name": "@lotusflare/backend",
  "version": "0.4",
  "private": true,
  "scripts": {
    "deploy": "bun run cf-typegen && pnpx wrangler deploy",
    "dev": "bun run cf-typegen && pnpx wrangler dev",
    "typecheck": "bun tsc --noEmit",
    "cf-typegen": "bun wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251117.0",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "hono": "^4.10.6",
    "zod": "catalog:"
  }
}
